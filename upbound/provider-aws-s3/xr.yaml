apiVersion: crossplane.jonashackt.io/v1
kind: ObjectStorage
metadata:
  namespace: default
  name: managed-upbound-s3
spec:
  parameters:
    bucketName: container-conf-bucket
    region: eu-central-1
